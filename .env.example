# Copy to .env and adjust as needed.

# Streamlit decode mode: "local" (decode in-process) or "api" (call a running FastAPI instance)
DECODER_MODE=local

# Only used when DECODER_MODE=api
API_URL=http://localhost:8000
